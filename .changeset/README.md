# Changesets

This folder is managed by `@changesets/cli`.

To add a changeset:
```bash
bun changeset
```

To version packages:
```bash
bun changeset version
```

To publish:
```bash
bun changeset publish
```
