---
"@dreki-gg/firestore-cli": minor
---

New package: `@dreki-gg/firestore-cli` — standalone Firestore CLI (`collections`, `query`, `get`, `count`, `relation-map`) backed by firebase-admin, with config resolution from `.agents/firestore.json` / `.pi/firestore.json` and digest-plus-temp-file output.
