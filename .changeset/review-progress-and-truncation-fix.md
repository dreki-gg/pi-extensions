---
"@dreki-gg/pi-code-reviewer": minor
---

Show review progress in the status bar during `/review` and `code_review` tool execution. Fix output truncation in `/review` by including the diff only once instead of duplicating it per lens.
